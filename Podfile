# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OpenTokSample' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OpenTokSample
  pod 'OTXCFramework', '2.25.3'
  
  target 'OpenTokSampleTests' do
    inherit! :complete
    # Pods for testing
  end

end
